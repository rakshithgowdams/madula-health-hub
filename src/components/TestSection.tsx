const TestServices = () => (
  <section className="py-20 bg-red-50">
    <div className="max-w-7xl mx-auto px-4">
      <h2 className="text-3xl font-bold">TEST: Services Section</h2>
      <p>If you can see this, sections render fine without motion.</p>
    </div>
  </section>
);

export default TestServices;
